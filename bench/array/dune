; (executable
;  (name timsort)
;  (libraries sorting_array genlib unix))

; (executable
;  (name peeksort)
;  (libraries sorting_array genlib unix))

(executable
 (name powersort)
 (libraries NearlyOptimalMergesort genlib unix))
